<wxs src="../shared/wxs/utils.wxs" module="utils" />
<wxs src="./flex.wxs" module="computed" />

<view
  id="{{ kId }}"
  style="{{ computed.cardStyle({ style, gap }) }}"
  class="k-class {{ computed.cardClass({justify, wrap, align, gap, vertical}) }} "
>
  <slot />
</view>
